def extract_field_note(note_text, field_name):
    """
    Takes in a note and returns the field value
    """
    # if field that needs onc hist:
        # potentially cache?
        # helper to parse onc history

    # if field_name = dx_date
        # logic for parsing dx_date (dx_date_parser(note_text))
        # return dx_date val

    